# RiceGrainNet
This application uses a Convolutional Neural Network (CNN) trained on thousands of rice grain images to classify different varieties of rice.

Try the model at:
https://huggingface.co/spaces/prxt/RiceGrainNet
